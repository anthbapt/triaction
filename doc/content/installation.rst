Installation
=================================================

Commands for a quick installation:

.. code-block:: bash

    conda create --name triaction python=3.10 -y
    python3 -m pip install triaction
